using System;

namespace SingleResponsibilityPrinciple
{
    public class AddressVerifier
    {
        public bool IsAddressValid(Address address)
        {
            //To control the address we need to whole account class.
            return true;
        }
    }
}